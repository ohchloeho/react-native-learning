import { colors } from "../themes/colors";
import { fontSizes, marginSizes } from "../themes/sizes";

const fontWeights = {
  light: 200,
  normal: 400,
  heavy: 600,
}

export const theme = {
  colors,
  fontSizes,
  fontWeights,
  marginSizes,
};
