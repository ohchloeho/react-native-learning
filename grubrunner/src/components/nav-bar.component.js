import React from "react";
import { View, Text } from "react-native";
import styled from "styled-components/native";

const NavBarContainer = styled()

export const NavBar = () => {
  return <View></View>;
};
